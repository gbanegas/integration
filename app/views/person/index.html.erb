<h1>Listing Person</h1>
 
<table>
  <tr>
    <th>Name</th>
    <th>Last Name</th>
  </tr>
 
  <% @person.each do |person| %>
    <tr>
      <td><%= person.name %></td>
      <td><%= person.last_name %></td>
    </tr>
  <% end %>
</table>
